# Archived docs (historical only)

These files are kept for historical reference only and may be outdated, incomplete, or superseded by canonical documentation in `docs/`.

If a topic exists in both `docs/` and `docs/archive/`, always use the non-archived document as the source of truth.
